# Nginx configuration for Sanjana CRM
# This file should be placed in your nginx sites-available directory
# Usually: /etc/nginx/sites-available/sanjana-crm
# Then symlink to: /etc/nginx/sites-enabled/sanjana-crm

server {
    listen 80;
    server_name prod.sanjanawaterproofing.com;

    # Increase client max body size for APK uploads (150MB)
    client_max_body_size 150M;
    
    # Increase buffer sizes for large uploads
    client_body_buffer_size 128k;
    client_header_buffer_size 1k;
    large_client_header_buffers 4 16k;

    # Proxy settings
    proxy_connect_timeout 300;
    proxy_send_timeout 300;
    proxy_read_timeout 300;
    send_timeout 300;

    # Frontend (React app)
    location / {
        root /var/www/sanjana-crm/frontend/dist;
        try_files $uri $uri/ /index.html;
        
        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
    }

    # Backend API
    location /api {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Increase timeouts for large file uploads
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # Static files (uploads)
    location /uploads {
        alias /var/www/sanjana-crm/uploads;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;
}

# HTTPS configuration (if using SSL)
# server {
#     listen 443 ssl http2;
#     server_name prod.sanjanawaterproofing.com;
# 
#     # SSL certificates
#     ssl_certificate /etc/letsencrypt/live/prod.sanjanawaterproofing.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/prod.sanjanawaterproofing.com/privkey.pem;
# 
#     # Increase client max body size for APK uploads (150MB)
#     client_max_body_size 150M;
#     
#     # Increase buffer sizes for large uploads
#     client_body_buffer_size 128k;
#     client_header_buffer_size 1k;
#     large_client_header_buffers 4 16k;
# 
#     # Proxy settings
#     proxy_connect_timeout 300;
#     proxy_send_timeout 300;
#     proxy_read_timeout 300;
#     send_timeout 300;
# 
#     # Frontend (React app)
#     location / {
#         root /var/www/sanjana-crm/frontend/dist;
#         try_files $uri $uri/ /index.html;
#         
#         # Security headers
#         add_header X-Frame-Options "SAMEORIGIN" always;
#         add_header X-Content-Type-Options "nosniff" always;
#         add_header X-XSS-Protection "1; mode=block" always;
#     }
# 
#     # Backend API
#     location /api {
#         proxy_pass http://localhost:5000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         
#         # Increase timeouts for large file uploads
#         proxy_connect_timeout 300s;
#         proxy_send_timeout 300s;
#         proxy_read_timeout 300s;
#     }
# 
#     # Static files (uploads)
#     location /uploads {
#         alias /var/www/sanjana-crm/uploads;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# 
#     # Gzip compression
#     gzip on;
#     gzip_vary on;
#     gzip_min_length 1024;
#     gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;
# }

